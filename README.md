# concept-lab

Personal concept catalog for learning math, statistics, machine learning, and neighboring topics through MDX notes with Japanese narrative text, LaTeX math, and interactive visualizations.

## Commands

- `npm run dev` - start the local Astro dev server
- `npm run build` - build the static site and generate the Pagefind index
- `npm run preview` - preview the built site locally
